## Agent skills

### Issue tracker

工单以 `.scratch/<feature-slug>/` 下 Markdown 文件跟踪。详见 `docs/agents/issue-tracker.md`。

### Triage labels

分拣标签使用默认五角色字符串。详见 `docs/agents/triage-labels.md`。

### Domain docs

多语境布局：工作区根 `CONTEXT-MAP.md` 指向各子项目 `CONTEXT.md` 与 `docs/adr/`。详见 `docs/agents/domain.md`。
