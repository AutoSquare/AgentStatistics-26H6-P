# Icons

AgentStatistics icon assets live here.

- `logo.ico`: WPF application and window icon.
- `logo.png`: source image copied to `WebClient/public/logo.png` for the web dashboard brand mark.

When `logo.png` is changed manually, copy it to `WebClient/public/logo.png` before rebuilding the web client.
