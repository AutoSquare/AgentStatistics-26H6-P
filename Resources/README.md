# Resources

Place WPF assets here. Use ASCII paths only.

- `icons/` — toolbar, ErrorProvider alarm icons
- `backgrounds/` — splash / about images
- `fonts/` — embedded font families referenced from App.xaml

Copy rules are wired in csproj (`dual-stack: resources`).
